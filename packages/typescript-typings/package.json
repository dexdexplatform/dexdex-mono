{
  "name": "@dexdex/typescript-typings",
  "version": "0.3.0",
  "description": "Typings",
  "license": "MIT",
  "scripts": {
    "precommit": "lint-staged",
    "clean": "echo 'No clean required'",
    "build": "echo 'No build required'",
    "watch": "echo 'No watch support'",
    "test": "echo 'No tests'"
  },
  "lint-staged": {
    "src/**/*.{js,jsx,ts,tsx,json,css}": [
      "prettier -l"
    ]
  },
  "devDependencies": {
    "lint-staged": "^7.2.0"
  }
}
