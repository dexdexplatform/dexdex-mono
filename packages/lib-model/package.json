{
  "name": "@dexdex/model",
  "version": "0.0.8",
  "description": "DexDex Model",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "precommit": "lint-staged",
    "clean": "rm -rf lib",
    "prebuild": "yarn clean",
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "prepublishOnly": "yarn test && yarn build"
  },
  "lint-staged": {
    "src/**/*.{js,jsx,ts,tsx,json,css}": [
      "prettier -l"
    ]
  },
  "devDependencies": {
    "@dexdex/typescript-typings": "^0.0.7",
    "@dexdex/utils": "^0.0.7",
    "@types/jest": "^22.2.3",
    "jest": "^23.0.1",
    "lint-staged": "^7.1.2",
    "typescript": "^2.8.3"
  },
  "dependencies": {
    "bn.js": "^4.11.8",
    "ethjs-unit": "^0.1.6",
    "tslib": "^1.9.1"
  }
}
